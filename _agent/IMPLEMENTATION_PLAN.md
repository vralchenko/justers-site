# План реалізації виправлень сайту ДЖАСТЕРС

> Статус: 🔲 Очікує виконання  
> Дата складання: 2026-02-20

---

## 📋 Зміст

1. [Мовні та лексичні виправлення](#1-мовні-та-лексичні-виправлення)
2. [Форматування контактних даних](#2-форматування-контактних-даних)
3. [Технічні виправлення](#3-технічні-виправлення)
4. [Мобільна адаптивність](#4-мобільна-адаптивність)
5. [UI/UX покращення](#5-uiux-покращення)
6. [Графіка та зображення](#6-графіка-та-зображення)

---

## 1. Мовні та лексичні виправлення

**Файл:** `index.html`

### 1.1 Виправлення пунктуації у модальному вікні callback

- **Де:** рядок 400 — `<h2 class="modal-title modal-title-callback">`
- **Було:** `Залишіть свій номер,<br>і ми Вам зателефонуємо.`
- **Стало:** `Залишіть свій номер, <br>і ми Вам зателефонуємо.`
- **Причина:** Після коми обов'язковий пробіл перед `<br>` (хоча `<br>` і переносить рядок, пробіл важливий для семантичної коректності та копіювання тексту).

### 1.2 Виправлення зайвих ком у відгуку Dima Anhlikovskyi

- **Де:** рядок 272–275 — текст відгуку
- **Було:** `«Вже вкотре, звертаюся за допомогою, до юридичної компанії...`
- **Стало:** `«Вже вкотре звертаюся за допомогою до юридичної компанії...`
- **Причина:** Коми розривають єдиний смисловий ланцюг речення і є граматично зайвими.

### 1.3 Заміна "виграних справ" на більш професійний термін

- **Де:** рядки 104, 168 — `<span class="stat-label-inline">` та `.stat-label`
- **Було:** `виграних справ`
- **Стало:** `успішних справ`
- **Причина:** "Виграних справ" — розмовний термін. "Успішних справ" / "позитивних рішень" — більш прийнятне для юридичної практики формулювання.
- **Також:** оновити `<meta name="description">` (рядок 9) — замінити "80%+ виграних справ" → "80%+ успішних справ"

---

## 2. Форматування контактних даних

**Файл:** `index.html`

### 2.1 Привести номери телефонів у хедері до міжнародного формату

- **Де:** рядки 55–56 — блок `.contact-phones`
- **Було:**
  ```html
  <div>067 380 17 77</div>
  <div>067 456 00 11</div>
  ```
- **Стало:**
  ```html
  <div>+38 (067) 380-17-77</div>
  <div>+38 (067) 456-00-11</div>
  ```

### 2.2 Привести номери телефонів у секції Контакти до міжнародного формату

- **Де:** рядок 326 — `.contact-text-block`
- **Було:** `<p>офіс тел. 80674560011, 80673801777</p>`
- **Стало:**
  ```html
  <p>
    <i class="fa-solid fa-phone"></i>
    Тел.: <a href="tel:+380674560011">+38 (067) 456-00-11</a>,
    <a href="tel:+380673801777">+38 (067) 380-17-77</a>
  </p>
  ```
- **Причина:** Формат `8067...` некоректний для міжнародних клієнтів, відсутній `+` та дужки. Додаємо іконку та `<a href="tel:...">` для клікабельності на мобільних.

### 2.3 Додати іконки до контактного блоку (адреса, email)

- **Де:** рядки 324–327 — `.contact-text-block`
- **Стало:** кожен `<p>` отримує іконку FontAwesome перед текстом:
  - 🏢 Назва: `fa-scale-balanced`
  - 📍 Адреса: `fa-location-dot`
  - 📞 Телефон: `fa-phone`
  - ✉️ Email: `fa-envelope`

---

## 3. Технічні виправлення

### 3.1 Якірна навігація — `scroll-margin-top` для секцій

- **Файл:** `css/styles.css`
- **Проблема:** При переході по анкору `#reviews`, `#contacts` тощо хедер перекриває заголовок секції.
- **Де:** Вже є `scroll-padding-top: 80px` на `html` (рядок 23) — але для надійності додати явний `scroll-margin-top` до кожної `.page` секції.
- **Дія:** Додати до `.page` в `styles.css`:
  ```css
  scroll-margin-top: 80px;
  ```

### 3.2 Валідація та маска поля телефону у формах

- **Файл:** `js/script.js`
- **Поточний стан:** Форматування вже реалізовано (рядки 238–281). Потрібно перевірити коректність і виправити:
  - Функція `formatPhoneNumber` форматує як `+ 38 (XX) XX XX XXXX` — потрібно узгодити зі стандартом `+38 (067) 380-17-77`.
  - Дефіси замість пробілів у форматі номера.
- **Дія:** Оновити функцію форматування та placeholder у полі `type="tel"` (рядки 386, 409):
  - Placeholder: `+38 (___) ___-__-__`

### 3.3 Favicon

- **Файл:** `index.html` (`<head>`)
- **Проблема:** Відсутній favicon — сайт виглядає непрофесійно у вкладках браузера.
- **Дія:** Додати рядок у `<head>`:
  ```html
  <link rel="icon" type="image/png" href="images/logo.png">
  ```
  - В ідеалі замінити `logo.png` на SVG або ICO пізніше.

---

## 4. Мобільна адаптивність

**Файл:** `css/responsive.css`

### 4.1 Мінімальні відступи від країв екрана

- **Де:** `@media (max-width: 480px)`
- **Поточний стан:** `.container` має `padding: 0 15px` — це мінімум, але деякі внутрішні елементи можуть "прилипати".
- **Дія:** Перевірити та при потребі додати `padding-left/right: 16px` до `.hero-text`, `.about-text`, `.contact-text-block` окремо для екранів < 375px:
  ```css
  @media (max-width: 375px) {
    .container {
      padding: 0 16px;
    }
    .hero-title {
      font-size: 26px;
      white-space: normal;
    }
  }
  ```

### 4.2 `white-space` для заголовків (проблема злиття слів)

- **Де:** `css/styles.css` — `.hero-title`, `css/responsive.css`
- **Проблема:** "АДВОКАТСЬКИЙЗАХИСТ" зливається на вузьких екранах.
- **Дія:** У `index.html` заголовок вже розбитий `<br>`:
  ```html
  <h1 class="hero-title">АДВОКАТСЬКИЙ<br>ЗАХИСТ</h1>
  ```
  Проте якщо `<br>` ігнорується, додати до `styles.css`:
  ```css
  .hero-title {
    word-break: keep-all;
    overflow-wrap: normal;
  }
  ```

### 4.3 Розмір кнопок (touch target 44x44px)

- **Де:** `css/styles.css` + `css/responsive.css`
- **Проблема:** Кнопки "Надіслати" та кнопка-дзвінок мають малу зону натискання на мобільних.
- **Дія:** Для мобільних додати у `@media (max-width: 768px)`:
  ```css
  .modal-form .btn {
    min-height: 52px;
    width: 100%;
    font-size: 17px;
  }
  .call-btn-mockup {
    padding: 10px;
    min-height: 44px;
  }
  ```

### 4.4 CTA-кнопка "Отримати консультацію" — розрив тексту

- **Де:** `css/styles.css` — `.btn-hero-cta` + `index.html` рядок 112
- **Поточний стан:** Кнопка містить `<br>` між словами, що виглядає некрасиво.
- **Варіант А (рекомендований):** Прибрати `<br>`, зробити кнопку ширшою:
  ```html
  <a href="#contacts" class="btn btn-hero-cta">ОТРИМАТИ КОНСУЛЬТАЦІЮ</a>
  ```
  ```css
  .btn-hero-cta {
    min-width: 280px;
    white-space: nowrap;
  }
  @media (max-width: 480px) {
    .btn-hero-cta {
      width: 100%;
      white-space: normal;
      font-size: 15px;
    }
  }
  ```

---

## 5. UI/UX покращення

### 5.1 Однакова висота карток відгуків

- **Файл:** `css/styles.css`
- **Проблема:** Різна довжина цитат → різна висота карток → некрасивий вигляд гриду.
- **Дія:** Додати до `.review-card`:
  ```css
  .review-card {
    display: flex;
    flex-direction: column;
    min-height: 200px;
  }
  .review-text {
    flex: 1;
  }
  ```

### 5.2 `line-height` у блоках "Про нас" та "Послуги"

- **Файл:** `css/styles.css`
- **Поточний стан:** `.about-text p` має `line-height: 1.8` — добре. `.service-desc` має `line-height: 1.6` — добре.
- **Дія:** Перевірити `.feature-desc` — поточне `1.4` (рядок 531) — збільшити до `1.55`.

### 5.3 Акцент та тінь на CTA-кнопці

- **Файл:** `css/styles.css`
- **Дія:** Додати до `.btn-hero-cta` легкий box-shadow для "преміальнішого" вигляду:
  ```css
  .btn-hero-cta {
    box-shadow: 0 4px 20px rgba(255, 255, 255, 0.15);
  }
  .btn-hero-cta:hover {
    box-shadow: 0 8px 30px rgba(255, 255, 255, 0.3);
  }
  ```

### 5.4 Іконки у футері/контактному блоці

- **Файл:** `index.html` + `css/styles.css`
- **Дія:** Стилізувати іконки у `.contact-text-block`:
  ```css
  .contact-text-block p {
    display: flex;
    align-items: flex-start;
    gap: 10px;
    margin-bottom: 12px;
    color: var(--text-muted);
    font-size: 16px;
  }
  .contact-text-block p i {
    color: var(--accent);
    margin-top: 3px;
    flex-shrink: 0;
    width: 20px;
  }
  .contact-text-block a {
    color: var(--text-muted);
    transition: color 0.3s;
  }
  .contact-text-block a:hover {
    color: var(--accent);
  }
  ```

---

## 6. Графіка та зображення

### 6.1 Логотип — SVG замість PNG (Retina)

- **Файл:** `index.html`, рядок 31 + `css/styles.css` рядок 135
- **Проблема:** `logo.png` може виглядати розмито на Retina (2x) дисплеях.
- **Дія:**
  1. Конвертувати `logo.png` → `logo.svg` (або отримати SVG від замовника).
  2. Оновити посилання: `<img src="images/logo.svg" ...>`.
  3. У CSS: `background: url('../images/background-header.png')` — розглянути оптимізацію через WebP.
- **Пріоритет:** Середній (потребує наявності SVG-версії логотипу).

### 6.2 Оптимізація зображень фону

- **Файли:** `images/*.png`
- **Проблема:** `background-about.png` важить 2.3 MB — критично для мобільного інтернету.
- **Дія:**
  1. Конвертувати всі фонові зображення у WebP (стиснення ~60-80% без втрати якості).
  2. Оновити посилання в CSS.
  3. Для `background-about.png` використати `image-set()` або `<picture>` якщо це буде `<img>`.
- **Команда для конвертації (cwebp):**
  ```bash
  cwebp images/background-about.png -o images/background-about.webp -q 80
  cwebp images/background-hero.png -o images/background-hero.webp -q 80
  cwebp images/background-services.png -o images/background-services.webp -q 80
  ```

---

## 🗂️ Пріоритети виконання (черга)

| # | Задача | Файл | Пріоритет | Складність |
|---|--------|------|-----------|------------|
| 1 | Форматування телефонів (+38...) | `index.html` | 🔴 Критичний | Низька |
| 2 | Іконки + клікабельні `<a href="tel:">` у контактах | `index.html` | 🔴 Критичний | Низька |
| 3 | Виправлення пунктуації у відгуку та callback | `index.html` | 🔴 Критичний | Низька |
| 4 | Заміна "виграних справ" → "успішних справ" | `index.html` | 🟡 Середній | Низька |
| 5 | Favicon | `index.html` | 🟡 Середній | Низька |
| 6 | `scroll-margin-top` для секцій | `styles.css` | 🟡 Середній | Низька |
| 7 | Кнопка CTA без `<br>`, `white-space: nowrap` | `index.html` + `styles.css` | 🟡 Середній | Низька |
| 8 | Touch target кнопок (мобільні) | `responsive.css` | 🟡 Середній | Низька |
| 9 | Медіазапит для 375px (відступи, заголовки) | `responsive.css` | 🟡 Середній | Низька |
| 10 | Мін. висота карток відгуків | `styles.css` | 🟢 Бажаний | Низька |
| 11 | `line-height` feature-desc → 1.55 | `styles.css` | 🟢 Бажаний | Низька |
| 12 | Box-shadow на btn-hero-cta | `styles.css` | 🟢 Бажаний | Низька |
| 13 | SVG логотип | `images/` + html/css | 🟢 Бажаний | Середня |
| 14 | WebP фонові зображення | `images/` + css | 🟢 Бажаний | Середня |

---

## ✅ Чеклист виконання

- [x] **Крок 1:** Виправлення лексики та пунктуації (`index.html`)
- [x] **Крок 2:** Форматування контактів + іконки (`index.html`)
- [x] **Крок 3:** Favicon (`index.html`)
- [x] **Крок 4:** scroll-margin-top, feature-desc line-height, review-card min-height (`styles.css`)
- [x] **Крок 5:** CTA кнопка, box-shadow, touch targets (`styles.css` + `responsive.css`)
- [x] **Крок 6:** Медіазапит 375px (`responsive.css`)
- [x] **Крок 7:** Перевірка в браузері (DevTools → iPhone SE, 375px, 768px)
- [ ] **Крок 8:** SVG логотип (за наявності)
- [x] **Крок 9:** WebP конвертація зображень (−97% розміру, cwebp q=82)
- [x] **Крок 10:** Перевірка в браузері після WebP + Performance метрики
- [ ] **Commit та Push** (тільки після підтвердження замовника)
